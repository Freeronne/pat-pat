export namespace Routes {
  export const Home = { name: 'index', path: '/' };
  export const Gallery = { name: 'gallery', path: '/gallery' };
  export const People = { name: 'people', path: '/people' };
  export const Contact = { name: 'contact', path: '/contact' };
}
